# Changelog

## 2025-10-29 — نسخه ۲.۴.۷

### 🔧 به‌روزرسانی‌ها و رفع مشکلات

- حذف ابزارهای موقتی و پاک‌سازی workspace (اسکریپت‌های یک‌بار مصرف در `tools/`).
- جابه‌جایی فیلد `exam_type` از جدول `courses` به `exam_seats` و پر کردن مقادیر موجود (مهاجرت روی این سرور).
- بهبود پردازش فایل‌های اکسل: اعتبارسنجی هدر، نگاشت ستون‌ها، و رفع باگ تبدیل مقدارهای DateTime در اسکریپت‌های چک‌کننده.
- اصلاح نام جدول‌های مرحله‌بندی به `e-exams` و `k-exams` برای تفکیک آزمون‌های الکترونیکی و کتبی.
- ایمن‌سازی APIها: اضافه شدن منطق پشتیبان (fallback) برای خواندن `exam_type` تا در صورت ناسازگاری اسکیمای DB، درخواست‌ها ۵۰۰ نشوند.
- حذف ابزارهای مهاجرت از workspace و ثبت تغییرات در تاریخچه گیت.

---

## 2025-10-25 — نسخه ۲.۲.۴

### ✨ قابلیت جدید

- **بررسی کد ساد تکراری:**
  - افزودن validation برای شناسایی کد لایسنس `00000000000000000000000000000000`
  - این کد نشان‌دهنده ثبت قبلی کد ساد در سیستم دیگر است
  - پیام خطای مناسب: "این کد ساد قبلاً در سیستم دیگری ثبت شده است. امکان ثبت مجدد وجود ندارد."

- **بهبود تجربه کاربری:**
  - پس از دریافت خطای کد تکراری، مودال تنظیمات اولیه دوباره نمایش داده می‌شود
  - کاربر می‌تواند کد ساد صحیح را مجدداً وارد کند
  - دکمه "تلاش مجدد" به جای "باشه" نمایش داده می‌شود

- **توضیحات تکنیکی:**
  - بررسی در `API/updateConfig.php` انجام می‌شود
  - فیلد `alreadyRegistered: true` در پاسخ JSON برگردانده می‌شود
  - JavaScript در `showInitModal` این خطا را handle و مودال را دوباره نمایش می‌دهد

---

## 2025-10-25 — نسخه ۲.۲.۳

### 🐛 رفع باگ راه‌اندازی

- **اصلاح logic راه‌اندازی اولیه:**
  - اضافه شدن بررسی IsInit در ابتدای `license_guard_validate()`
  - اگر IsInit != 'YES' باشد، سیستم در حالت setup قرار می‌گیرد
  - در حالت setup، license guard bypass شده و دسترسی کامل داده می‌شود
  - این تغییر اجازه می‌دهد مودال showInitModal بدون مشکل اجرا شود
  
- **حذف تابع اضافی:**
  - حذف `license_guard_request_auto_trial()` که در flow اشتباه قرار داشت
  - لایسنس فقط از طریق `updateConfig.php` پس از وارد کردن SaadCode دریافت می‌شود
  
- **توضیح flow صحیح:**
  1. کانتینر Docker اجرا می‌شود (IsInit=NO)
  2. کاربر به صفحه اصلی می‌رود (license guard در setup mode)
  3. مودال showInitModal نمایش داده می‌شود
  4. کاربر SaadCode و University را وارد می‌کند
  5. updateConfig.php لایسنس را از webhook دریافت و ذخیره می‌کند
  6. IsInit به YES تغییر می‌کند
  7. از این پس license guard فعال می‌شود

---

## 2025-10-25 — نسخه ۲.۲.۲

### 🐛 رفع باگ بحرانی

- **تصحیح درخواست خودکار لایسنس آزمایشی:**
  - اصلاح تابع `license_guard_request_auto_trial()` برای ارسال صحیح پارامترها
  - تغییر از POST به GET request با query parameters: `SaadCode` و `Center`
  - خواندن `SaadCode` و `University` از جدول Config قبل از ارسال درخواست
  - تطابق کامل با روش ارسال در `updateConfig.php`
  - اصلاح نام فیلد پاسخ از 'token' به 'Code' (مطابق webhook)
  - افزودن validation برای اطمینان از وجود SaadCode و University
  - رفع خطای "سرور لایسنس پاسخ نامعتبر داد" در نصب‌های جدید

- **توضیحات تکنیکی:**
  - قبلاً درخواست POST بدون body ارسال می‌شد که باعث خطا می‌شد
  - اکنون GET request با `?SaadCode=XXXX&Center=YYY` ارسال می‌شود
  - این تغییر باعث می‌شود نصب‌های جدید Docker بدون دخالت کاربر فعال شوند

---

## 2025-10-25 — نسخه ۲.۲.۱

### 🐛 رفع باگ‌ها

- **رفع مشکل لایسنس در اولین اجرا:**
  - افزودن تابع `license_guard_request_auto_trial()` برای درخواست خودکار لایسنس موقت
  - اگر `LicenseToken` در دیتابیس خالی باشد، سیستم خودکار به `https://wfa.pnubijar.ac.ir/webhook/Licence` درخواست POST می‌فرستد
  - توکن دریافتی در دیتابیس ذخیره می‌شود و برای اعتبارسنجی بعدی استفاده می‌شود
  - حذف خطای "License token is missing" که در اولین اجرای کانتینر Docker رخ می‌داد

- **بهبود Audit Logging:**
  - ثبت درخواست‌های لایسنس خودکار با جزئیات (موفق/ناموفق)
  - نمایش 10 کاراکتر اول توکن در لاگ برای امنیت

### 🔧 بهبودها

- **خطاهای بهتر:**
  - پیام‌های خطای فارسی برای مشکلات ارتباط با سرور لایسنس
  - Timeout مناسب برای درخواست‌های لایسنس (10 ثانیه)

---

## 2025-10-25 — نسخه ۲.۲.۰

### ✨ قابلیت‌های جدید

- **گزارش‌های تعاملی داشبورد:**
  - کلیک روی کارت "تعداد دانشجویان" برای جستجو و نمایش گزارش دانشجو
    - ورود شماره دانشجویی
    - نمایش مشخصات دانشجو (نام، کد ملی، مقطع)
    - نمایش جدول آزمون‌های دانشجو با شماره صندلی و محل استقرار
  - کلیک روی کارت "تعداد آزمون‌ها" برای جستجو و نمایش گزارش درس
    - ورود کد درس
    - نمایش مشخصات درس (نام، تاریخ، ساعت، نوع)
    - نمایش جدول دانشجویان درس به ترتیب نام خانوادگی
  - کلیک روی کارت "آزمون بعدی" برای نمایش گزارش جلسه آزمون
    - نمایش تعداد دروس و دانشجویان
    - لیست دروس این جلسه آزمون با تعداد شرکت‌کنندگان
    - جدول کامل دانشجویان با کد درس، نام درس و محل استقرار

- **کارت گزارش درخواستی:**
  - نمایش یکپارچه تمام گزارش‌ها در یک کارت مخفی
  - دکمه "پاک کردن گزارش" برای خالی کردن کارت
  - Scroll خودکار به کارت پس از نمایش گزارش

### 🎨 بهبود رابط کاربری

- **حذف اسکرول بار:**
  - حذف اسکرول بار از input های SweetAlert
  - حذف محدودیت ارتفاع کارت گزارش (گسترش کامل)
  
- **بهبود نمایش:**
  - نمایش تعداد شرکت‌کنندگان هر درس با badge خاکستری
  - Badge های رنگی برای نوع آزمون (کتبی/الکترونیکی)
  - جداول responsive با استایل Bootstrap پیشرفته

### 🔧 API های جدید

- **getStudentReport.php:**
  - دریافت اطلاعات دانشجو با شماره دانشجویی
  - لیست آزمون‌های دانشجو به ترتیب تاریخ و ساعت
  - شامل تمام جزئیات استقرار و شماره صندلی

- **getCourseReport.php:**
  - دریافت اطلاعات درس با کد درس
  - لیست دانشجویان به ترتیب نام خانوادگی
  - شامل مشخصات کامل دانشجویان و محل استقرار

- **getNextExamReport.php:**
  - دریافت اطلاعات جلسه آزمون با تاریخ و ساعت
  - لیست تمام دروس همزمان با تعداد شرکت‌کنندگان
  - جدول دانشجویان با اطلاعات درس و استقرار

### 🔐 امنیت

- **احراز هویت و مجوز:**
  - تمام API های گزارش نیازمند احراز هویت Admin
  - بررسی License Guard در تمام endpoint ها
  - مدیریت خطای لایسنس با پیام‌های فارسی

---

## 2025-10-25 — نسخه ۲.۱.۶

### 🐛 رفع باگ‌ها

- **رفع خطای Session Warning:**
  - اضافه کردن `session_start()` در ابتدای تمام فایل‌های ورودی
  - حل مشکل "Session cannot be started after headers have already been sent"
  - اطمینان از شروع session قبل از هر خروجی یا header

---

## 2025-10-25 — نسخه ۲.۱.۵

### 🔐 امنیت

- **سیستم حفاظت جامع:**
  - پیاده‌سازی CSRF Protection برای تمام درخواست‌های تغییر وضعیت
  - افزودن Rate Limiting برای جلوگیری از حملات Brute-force
  - پیاده‌سازی Internal Authentication برای APIهای حساس
  - بهبود مدیریت خطا و حذف @ error suppression
  - سخت‌تر کردن SSL verification در وب‌هوک لایسنس
  - افزودن Audit Logging برای رویدادهای امنیتی

- **محافظت API:**
  - حفاظت از تمام API endpoints با License Guard
  - اعتبارسنجی دسترسی مدیر در APIهای مدیریتی
  - محدودسازی فراخوانی‌های updateLicenseStatus به سرور داخلی

### 📤 قابلیت آپلود فایل دیتابیس

- **رابط کاربری آپلود:**
  - دکمه‌های فعال برای آپلود آزمون‌های کتبی و الکترونیکی
  - پاپ‌آپ زیبا با قابلیت Drag & Drop
  - نمایش Progress Bar در حین آپلود
  - طراحی مدرن با افکت‌های hover و dragover

- **مدیریت فایل:**
  - پذیرش فقط فایل‌های XLS و XLSX
  - اعتبارسنجی MIME type و حجم فایل
  - تشخیص خودکار حداکثر حجم از تنظیمات PHP
  - ذخیره‌سازی با نام‌گذاری استاندارد (K.xlsx برای کتبی، E.xlsx برای الکترونیکی)
  - ثبت لاگ آپلودها

- **امنیت آپلود:**
  - محافظت با CSRF و License Guard
  - بررسی احراز هویت مدیر
  - محدودیت 128MB (قابل تنظیم از PHP config)

### 🎨 بهبودهای رابط کاربری

- دکمه‌های آپلود با استایل سبز و افکت‌های زیبا
- ناحیه آپلود با border سبز و گرادیانت
- دکمه "انتخاب فایل" با متن فارسی
- نمایش نام فایل انتخاب شده

### 🛠️ بهبودهای فنی

- API جدید getUploadLimit.php برای دریافت محدودیت آپلود
- Helper functions برای تبدیل و فرمت کردن اندازه فایل
- مستثنی کردن فایل‌های آپلودی از Git

### 📝 مستندات

- افزودن SECURITY_IMPROVEMENTS.md با جزئیات کامل بهبودهای امنیتی
- به‌روزرسانی README.md
- مستندسازی سیستم آپلود فایل

---

## 2025-10-23 — نسخه ۲.۰.۱

### 🎯 بهبودها

- **داشبورد مدیریت:**
  - نمایش اطلاعات نزدیک‌ترین آزمون به‌جای تعداد کل صندلی‌ها
  - فرمت جدید: تعداد دانشجو (بالا) + ساعت | تاریخ (پایین)
  - محاسبه هوشمند نزدیک‌ترین آزمون با تبدیل تاریخ شمسی به timestamp
  - فیلتر خودکار آزمون‌های گذشته
  - نمایش فقط آزمون‌هایی که دانشجو دارند

- **طراحی:**
  - اعمال Glassmorphism به تمام کارت‌های داشبورد
  - جابجایی کارت عملیات سریع و اطلاعات لایسنس
  - حذف آیکون آواتار کاربر از هدر
  - شفاف‌سازی پس‌زمینه جدول در کارت اطلاعات لایسنس
  - غیرفعال کردن موقت دکمه‌های مدیریت آزمون

- **داده‌های نمونه:**
  - یکسان‌سازی داده‌های اولیه در `schema.sql` و `db_init.php`
  - به‌روزرسانی تاریخ‌های آزمون به آبان ۱۴۰۴

### 📝 مستندات

- به‌روزرسانی متن "درباره برنامه" در داشبورد

---

## 2025-10-23 — نسخه ۲.۰.۰

### 🎉 قابلیت‌های اصلی جدید

- **پنل مدیریت (Admin Dashboard)**
  - ورود مدیریتی با احراز هویت دو مرحله‌ای
    * نام کاربری: admin + کد مرکز (SaadCode)
    * رمز عبور: رشته معکوس شماره سریال (LicenseToken)
  - مدیریت نام نمایشی ادمین (AdminNickName)
  - داشبورد مدیریت در مسیر `/dashboard/`
  
- **آمار و گزارش‌گیری**
  - نمایش تعداد دانشجویان، دروس و صندلی‌ها
  - اطلاعات سیستم (دانشگاه، کد مرکز، شماره سریال)
  - شماره سریال با blur امنیتی (نمایش با hover)
  
- **رابط کاربری داشبورد**
  - طراحی یکپارچه با صفحه اصلی
  - پس‌زمینه ثابت با موج‌های انیمیشن
  - فوتر با نمایش نام دانشگاه
  - دکمه خروج و بروزرسانی اطلاعات

### 🔐 امنیت و احراز هویت

- سیستم لاگین همکار/ادمین
- ذخیره session با cookie (1 روز)
- محافظت از مسیر dashboard با بررسی session
- پنهان‌سازی شماره سریال با افکت blur

### 🎨 بهبود رابط کاربری

- فعال‌سازی دکمه رادیویی "همکار" در فرم لاگین
- ذخیره انتخاب نوع کاربر (دانشجو/همکار) در localStorage
- بهینه‌سازی نمایش فوتر در dashboard
- سایه متن برای خوانایی بهتر روی پس‌زمینه‌های مختلف

### 📚 API های جدید

- `API/getStatistics.php` - دریافت آمار سیستم
- `API/saveAdminNickName.php` - ذخیره نام نمایشی ادمین

### 🐛 رفع مشکلات

- رفع مشکل sanitization ورودی در حالت staff
- بهبود مدیریت کاراکترهای فارسی در نام کاربری ادمین
- حذف ساعت از فوتر dashboard برای سادگی بیشتر

---

## 2025-10-22 — نسخه ۱.۸.۰

### ✨ قابلیت‌های جدید

- **راه‌اندازی چند پلتفرمی (Multi-Platform Setup)**
  - اسکریپت نصب خودکار برای Linux (Ubuntu/Debian/CentOS/Fedora/Arch)
  - پشتیبانی از macOS با Homebrew
  - پشتیبانی از Windows با WSL/Git Bash
  - نصب خودکار Docker روی تمام پلتفرم‌ها
  - دانلود خودکار فایل‌های پروژه از GitHub
  - تولید رمز عبور امن با openssl
  - نصب در `/opt/Nesaar` برای Linux و `~/Nesaar` برای macOS/Windows

### 🔧 بهبودها

- **بهبود Docker Compose:**
  - استفاده از Environment Variables با مقادیر پیش‌فرض
  - اضافه شدن Watchtower برای آپدیت خودکار
  - تنظیم timezone MySQL روی Asia/Tehran (+03:30)
  - اضافه شدن health check برای MySQL
  - مسیرهای volume استاندارد شده به `/opt/Nesaar`

- **بهبود پایگاه داده:**
  - اضافه شدن UNIQUE constraint به ستون ConfigName
  - حذف فیلد Order که دیگر استفاده نمی‌شد
  - تنظیم timezone روی Asia/Tehran در PHP و MySQL
  - اسکریپت migration برای بروزرسانی دیتابیس‌های موجود

- **ساده‌سازی UI:**
  - حذف کامل کد مربوط به مدیریت نمایش لوگو
  - لوگو تنها بالای کارت لاگین نمایش داده می‌شود
  - حذف CSS مربوط به logo-container
  - کاهش پیچیدگی کد JavaScript

### 📚 مستندات

- **سازماندهی مستندات:**
  - انتقال CHANGELOG به پوشه `docs/`
  - ایجاد مستندات جامع برای License Cache & Grace Period
  - مستندات Smart Check و سیستم لایسنس
  - ساده‌سازی README با حذف بخش‌های فنی

### 🛠️ DevOps

- **اسکریپت Setup چند پلتفرمی:**
  - تشخیص خودکار سیستم عامل
  - نصب Docker بر اساس نوع سیستم
  - مدیریت Package Manager (apt/yum/pacman/brew)
  - خروجی رنگی و پیام‌های واضح
  - مدیریت خطا و بررسی دسترسی‌ها

### 🔒 امنیت

- تولید خودکار رمزهای امن در setup.sh
- محدودیت امنیتی Watchtower (no-new-privileges, read-only)
- استفاده از متغیرهای محیطی برای اطلاعات حساس

---

## 2025-10-22 — نسخه ۱.۷.۶

### ✨ قابلیت‌های جدید

- **پیاده‌سازی سیستم Cache و Grace Period برای لایسنس**
  - Grace Period 24 ساعته: در صورت عدم دسترسی به سرور لایسنس، تا 24 ساعت سیستم کار می‌کند
  - استفاده از cache تاریخ انقضا: اگر سرور در دسترس نباشد، از آخرین تاریخ انقضا استفاده می‌شود
  - Fallback هوشمند: در صورت نبود cache، اجازه دسترسی موقت داده می‌شود

### 🔧 بهبودها

- **مدیریت خطای سرور لایسنس:**
  - ذخیره آخرین وضعیت موفق بررسی (`LicenseLastSuccessCheck`)
  - ذخیره وضعیت هر بررسی (`LicenseLastStatus`: valid/invalid/error)
  - لاگ‌گذاری کامل تمام حالات با emoji برای debugging راحت‌تر
  
- **API های جدید:**
  - `API/updateLicenseStatus.php`: ذخیره وضعیت و تاریخ آخرین بررسی موفق
  - `API/getLicenseCache.php`: دریافت اطلاعات cache شده لایسنس

- **بهبود تجربه کاربری:**
  - پیام‌های شفاف‌تر در مورد وضعیت لایسنس
  - نمایش زمان باقیمانده Grace Period در console
  - کاهش قطعی سرویس به دلیل مشکلات شبکه

### 📚 مستندات

- اضافه شدن `docs/LICENSE_CACHE_GRACE_PERIOD.md` با توضیحات کامل:
  - فلوچارت تصمیم‌گیری سیستم
  - جدول سناریوهای مختلف
  - API Reference کامل
  - راهنمای debugging

### 🔒 امنیت

- تمام منطق در JavaScript obfuscated شده پنهان می‌ماند
- Grace Period محدود به 24 ساعت است
- لایسنس منقضی شده قابل استفاده نیست
- تمام رویدادها لاگ می‌شوند


## 2025-10-21 — نسخه ۱.۷.۵

- رفع کامل مشکل نمایش لوگو بعد از لاگین و refresh
- حذف تغییر کلاس‌های session از تابع clearResults
- بهبود مدیریت کلاس‌های CSS در تمام توابع


## 2025-10-21 — نسخه ۱.۷.۴

- رفع مشکل flickering لوگو هنگام بارگذاری صفحه
- بهبود لوجیک نمایش/مخفی‌سازی لوگو با استفاده از کلاس‌های صریح
- لوگو پیش‌فرض مخفی است و فقط برای کاربران غیر لاگین نمایش داده می‌شود


## 2025-10-21 — نسخه ۱.۷.۳

- پیاده‌سازی سیستم هوشمند بررسی لایسنس بر اساس نزدیکی به انقضا
- **لایسنس دائمی:** چک روزانه (یکبار در روز)
- **بیشتر از 1 روز تا انقضا:** چک روزانه (یکبار در روز)
- **کمتر از 24 ساعت تا انقضا:** چک ساعتی (هر 1 ساعت)
- **کمتر از 1 ساعت تا انقضا:** چک دقیقه‌ای (هر 1 دقیقه)
- اضافه شدن فیلد `LicenseExpiry` به Config (به صورت خودکار هنگام چک لایسنس آزمایشی)
- بهبود دقت و واکنش به تغییرات وضعیت لایسنس در لحظات حساس


## 2025-10-21 — نسخه ۱.۷.۲

- بهبود عملکرد بررسی لایسنس: تغییر از چک مداوم به چک روزانه
- کاهش 99.93% درخواست‌های بررسی لایسنس (از 1440 به 1 درخواست در روز)
- اضافه شدن کلید `LicenseLastChecked` به جدول Config
- اضافه شدن API endpoint جدید: `updateLicenseLastChecked.php`
- بهبود تجربه کاربری با کاهش زمان انتظار در refresh خودکار
- افزایش پایداری سیستم در صورت قطعی موقت سرور لایسنس


## 2025-10-21 — نسخه ۱.۷.۱

- انتقال بررسی لایسنس به سمت کلاینت (JavaScript) برای امنیت بیشتر
- پنهان‌سازی آدرس webhook لایسنس در کد obfuscated شده
- افزودن API endpoint جدید برای دریافت توکن لایسنس
- بهبود امنیت و کاهش بار سرور


## 2025-10-21 — نسخه ۱.۷.۰

- رفع مشکل جابجایی صفحه هنگام باز شدن SweetAlert (layout shift)
- اضافه شدن لینک تلگرام توسعه‌دهنده (@RealDream) در بخش درباره
- بهبود امنیت با obfuscation کدهای JavaScript
- بهینه‌سازی عملکرد و تجربه کاربری


## 2025-10-21 — نسخه ۱.۶.۰

- رفع ایرادات جزئی در نمایش ساعت
- فعال‌سازی فاز دوم سیستم لایسنسینگ
- بهبود طراحی فوتر با افکت شیشه‌ای ملایم


## 2025-10-21 — نسخه ۱.۵.۴

- رفع ایرادات جزئی در نمایش ساعت
- فعال‌سازی فاز دوم سیستم لایسنسینگ


## 2025-10-20 — نسخه ۱.۵.۲

- رفع ایرادات جزئی
- تغییر ساختار دکمه خروج


## 2025-10-19 — نسخه ۱.۵.۰

- رفع ایرادات جزئی


## 2025-10-17 — بهبود مدیریت آفلاین و سرویس‌ورکر

- بهبود تجربه کاربری در حالت قطع ارتباط با سرور:
	- حفظ session کاربر هنگام قطع اینترنت یا خطای سرور
	- تلاش پس‌زمینه برای اتصال مجدد و دریافت داده‌ها (هر ۱۰ ثانیه)
	- نمایش پیام و spinner تا زمان برقراری ارتباط
- به‌روزرسانی سرویس‌ورکر و نسخه کش


## 2025-10-16 — رفع ایراد‌های جزئی

-  هشدار استفاده از گزارش ۴۲۸ سامانه گلستان (نمایش در کارت‌های آینده)
-  نمایش ثانیه در فوتر (سینک‌شده با زمان سرور، مصرف شبکه بدون تغییر)
-  ثابت‌شدن عرض اعداد در فوتر با استفاده از tabular-nums برای جلوگیری از جابجایی عرض هنگام تغییر ثانیه
